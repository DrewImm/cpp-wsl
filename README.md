# C++ WSL Boilerplate

## Installation

1. Add dependencies in conanfile.txt
2. Download & build dependencies with `./install.sh`

## Build (Debug)

`./build-debug.sh`

## Build (Release)

`./build-release.sh`
